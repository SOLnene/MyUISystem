using UnityEngine;
namespace Game.UI.Components.CharacterDetail
{
    public class CharacterDetailFlowController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
