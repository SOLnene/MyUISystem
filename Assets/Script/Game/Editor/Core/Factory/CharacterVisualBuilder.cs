using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public static class CharacterVisualBuilder
{
   public static void Fill(
      CharacterVisualDefinition def,
      CharacterVisualType type,
      string address)
   {
      switch (type)
      {
         case CharacterVisualType.Icon:
            def.avatarIconPath = address;
            break;
         case CharacterVisualType.Image:
            def.avatarImagePath = address;
            break;
      }
   }
   
   public static bool IsComplete(CharacterVisualDefinition def)
   {
      return !string.IsNullOrEmpty(def.avatarIconPath)
             && !string.IsNullOrEmpty(def.avatarImagePath);
   }
}*/
