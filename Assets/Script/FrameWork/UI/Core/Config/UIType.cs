using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * UI种类,用于唯一ui界面
 */
public enum UIType
{
    UILoadingView,
    UIStartView,
		HubRoot,
		EquipDetailView,
		ItemSelectPopupView,
		GachaView,
		GachaResultPopup,
		GachaResultDetailPopup,
		GachaResultRevealView,
		CharacterDetailView,
		TipView,
		MainMenuView,
		BackpackView,
		StoreView,
		InfoPanelPopup,
		Max,
}